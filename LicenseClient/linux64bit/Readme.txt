1.将授权程序设置可执行权限；chmod +x license；
2.注册的时候，执行:
   license -r [filename.user]
	第三个参数是授权文件，如果为空，则默认为lingjoin.user
3.填报相关数据信息后，执行如下命令，
 license -a [filename.user]
4.按照提示将客户经理提供的SN号输入，在线激活成功后【需要能连上我们的授权服务器】，即可获取商用授权。
